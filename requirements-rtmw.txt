# RTMW whole-body pose backend (Phase 1 — opt-in)
#
# These are HEAVY (~2 GB total).  Only install if you want to use:
#   VIDEOANALYZER_BACKEND=rtmw
#
# Install order matters — mmcv must be built against your installed torch:
#
#   1. Make sure torch is already installed (setup.bat handles this).
#   2. From the project venv:
#        .venv\Scripts\python.exe -m pip install -r requirements-rtmw.txt
#   3. Set the backend and launch:
#        set VIDEOANALYZER_BACKEND=rtmw
#        .venv\Scripts\python.exe hand_heatmap_modern.py
#
# See VISION_UPGRADE_PLAN.md §4 / §5 for profile selection and hardware notes.

openmim>=0.3.9

# OpenMMLab core stack.  Versions are loosely pinned to known-good
# combinations with torch 2.6 / CUDA 12.4.
mmengine>=0.10.4,<1.0
mmcv>=2.1.0,<2.3
mmdet>=3.2.0,<4.0
mmpose>=1.3.0,<2.0

# Optional but recommended — used internally by mmpose's MMPoseInferencer
# when downloading the RTMW checkpoint from the OpenMMLab model zoo.
modelindex>=0.0.2
